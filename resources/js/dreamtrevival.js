const canvas = document.getElementById('gameCanvas');
const ctx = canvas.getContext('2d');

const tileSize = 32;

function update()
{
    // wake up 
}

function init()
{
    canvas.width = tileSize * 30;
    canvas.height = tileSize * 25;
}